{
  "name": "package",
  "version": "1.0.0",
  "description": "To start your Phoenix server:   * Initialize environment `make init`   * Install dependencies with `make dep`   * Run server `make dev`   * Test: `make test` Now you can visit [`localhost:4000`](http://localhost:4000) from your browser.",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "dependencies": {},
  "devDependencies": {
    "@commitlint/cli": "^9.1.1",
    "@commitlint/config-conventional": "^9.1.1",
    "husky": "^4.2.5"
    
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://git.d.foundation/sci/backend.git"
  },
  "author": "",
  "license": "ISC"
}
